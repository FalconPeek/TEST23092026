RESULT: PASS

## Checks (T-028 checked against the full T-018 criteria)
- `npx vitest run lib/tournament components/tournament`: 5 files, 47 tests passed, including the 4 new champion cases.
- `npm run lint`: clean. `npx tsc --noEmit`: clean. `npm test`: 719 passed, 3 skipped, 71 files. `npm run build`: succeeded.
- Changed files are within the T-018/T-028 lists.
- The `resolveChampion` logic in `lib/tournament/bracket-layout.ts:150` is correct:
  - It looks at the last `final`-bracket match by round. If it's `completed`, that match's winner is the champion.
  - If it's `archived` (no reset needed), GF1's winner is the champion, but only if GF1 is completed.
  - Any pending status gives null.
  - Third place is a separate `third` bracket, so it never decides the champion.
- The earlier repro (LB team wins GF1, GF2 still `ready`) is now covered by a test and returns null. There are also tests for GF2 completed, GF2 archived, and a pending single-elim final.
- The llave page renders championName only when `tournament.status === "finished"` and the layout has a champion.
- The rest of T-018 holds (verified earlier):
  - Realtime subscribe/unsubscribe with a debounce.
  - Admin-only menu.
  - The bracket container scrolls only within itself.
  - The per-match winner comes from `winner_entry_id`.
