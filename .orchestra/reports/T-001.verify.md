RESULT: PASS

## Checks
- `npx vitest run lib/format.test.ts` → 20/20 pass. Also run with TZ=UTC → pass (the output doesn't depend on the machine's timezone).
- `eslint` on the 3 task files → clean. `npm run typecheck` → pass.
- `npm test` → 210 pass, 1 fail in `lib/brackets/double-elim.test.ts` ("no rematch … losers bracket"). That file is untracked and outside T-001's Files: the bracket engine is still being built by someone else. It isn't caused by this task. Note: `npm test` is not green repo-wide until that is fixed.
- Files: only `messages/es.ts`, `lib/format.ts`, `lib/format.test.ts` come from this task. Other untracked files (`lib/brackets/`, `debug.test.ts`, `debug2.test.ts`) are not from the worker. The debug*.test.ts files at the repo root should be deleted by whoever created them.
- Review: `es` is `as const` + `Messages` type. 15 positions and 34 attributes, with the exact keys and labels from the spec. Copy is in voseo. No `any` and no console.log. `Intl` formatters are cached at module level (except the non-default fraction-digit variants, which is acceptable). ISO string or Date accepted; invalid dates throw RangeError. Tests cover the TZ shift, past/future relative time, ayer/mañana, grouping and decimal comma.

## Notes (not failures)
- `formatDateLong` gives "miércoles, 23 de septiembre" (ICU adds a comma), not "miércoles 23 de septiembre" as the spec example shows. That's acceptable.
- A relative time under 1 minute shows "este minuto". Consider special-casing it as "ahora" later if the UX wants that.
- `formatDecimal` creates a new NumberFormat on every call. It could be cached per `digits`.
