# T-021 worker report

## Summary
Fixed the two tap-target findings from T-014's rejection: the stat-stepper +/- buttons
(`size-6` → `size-11`, with the row layout changed from one long horizontal strip to a
per-player card with a 2×2 stepper grid so 4 steppers still fit at 375px) and the standout
search input (`h-8` → `h-11`). Added regression tests for both.

## Files changed
- `components/match/stats-form.tsx` — `MiniStepper`'s two buttons are now `size-11` (44px).
  Restructured each player row from a single horizontal flex strip (avatar + name + up to 4
  inline steppers, which is what produced the undersized buttons in the first place — there
  wasn't room for 44px buttons ×4 plus an avatar and name on one line at 375px) into a
  stacked card: avatar+name on top, then a `grid-cols-2` grid of steppers below. Did the
  width math explicitly rather than eyeballing it: two 44px buttons + a 6px gap + a 24px
  value readout ≈ 118px per stepper cell; two cells + an 8px grid gap ≈ 244px, comfortably
  under the ~343px available inside the outer card's padding at a 375px viewport.
- `components/match/rating-form.tsx` — the standout-attribute search `Input` is now `h-11`.
- `components/match/stats-form.test.tsx` (new — T-021 asked for this as a new file, since
  T-014 had no stats-form test at all): a tap-target regression test asserting every
  stepper +/- button's `className` contains `size-11`; that the saves stepper shows for a
  `POR` player without touching the toggle and stays hidden for others; a functional test
  (click `+`, submit, assert the exact `submitStatReports` payload — only the touched
  nonzero row is sent); and the `goalsLoaded` live-check text once a score report exists.
- `components/match/rating-form.test.tsx` — added two regression tests: every rating button
  (1-10) carries `h-11`, and the standout search input carries `h-11`.

## Commands run
- `npx vitest run components/match/stats-form.test.tsx components/match/rating-form.test.tsx lib/match/report.test.ts` — 20/20 passed.
- `npm run lint` — 0 errors; 2 warnings, both in FATHER's in-progress
  `lib/actions/matches.ts` (unused notification-hook imports mid-edit), unrelated to this
  task.
- `npm run typecheck` — clean.
- `npm test` — 598 passed / 3 skipped (pre-existing DB-integration skips) across 62 files.
- `npm run build` — succeeded cleanly, including
  `/g/[groupId]/partidos/[matchId]` in the route list. The `@/lib/server/tournament-advance`
  resolution failure T-014's verify report and this task's own instructions warned about is
  gone — the FATHER subagent that owns that file must have landed a fix between then and now;
  nothing on my end needed to change for it.

## Manual verification
No new RPC-facing behavior in this fix (pure styling/layout + regression tests over
existing, already-verified logic from T-014's own manual verification pass). Did the tap-
target math by hand (see above) since the Chrome extension still isn't connecting in this
environment, consistent with every prior task.

## Known issues / assumptions
- Per T-021's explicit instructions, only this task's status was set to `done`; T-014's
  `status: rejected` was left untouched for the Verifier/FATHER to reconcile.
