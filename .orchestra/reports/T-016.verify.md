RESULT: PASS

## Checks
- `npx vitest run components/tournament lib/actions/errors.test.ts`: 2 files, 27 tests passed.
- `npm run lint`: clean. `npx tsc --noEmit`: clean. `npm run build`: succeeded, and /torneos and /torneos/nuevo are dynamic.
- `npm test`: 672 passed and 1 failed. The failure is `lib/actions/matches.test.ts > amendMatchStats`, which is FATHER's in-progress amend_match_stats work (the uncommitted migration 20260924173304 plus lib/actions/matches*). It isn't a worker file or a T-016 regression.
- Changed worker files are all declared in the task. The many other modified files (lib/badges, lib/reconcile, lib/server, database.types, the migration and pgTAP 031) are FATHER's.
- The 3 new PICADO codes are mapped to the specified strings, and errors.test.ts is extended.
- The list page shows name, format label, status badge and the entries/registrations count (via RLS embeds). "Nuevo torneo" is admin-only.
- /nuevo calls notFound() for non-admins.
- Form:
  - It shows only format-relevant settings; the show/hide per format is tested.
  - Tiebreakers are reordered with up/down buttons (tested), and swiss edits `swiss.tiebreakers`.
  - It runs `tournamentSettingsSchema.safeParse` before submit, blocks on invalid input with a toast (tested), and sends the parsed object. The server action re-parses it with defaults.
  - On success it goes to /torneos/[id]; team size defaults from the group settings.
- Copy comes from es.tournaments.* and es.errors.* in voseo. No `any`, no console.log.

## Notes (non-blocking; suggest adding to T-020 polish)
1. Field-level errors in create-tournament-form.tsx (lines ~121–124, rendered at 246/260/294) show the raw zod `issue.message` in English, e.g. "Too small: expected number to be >=1". group-settings-form.tsx (T-009) has the same pattern, which I missed there. Map these to Spanish copy, e.g. a generic `es.errors.validation` or range-specific strings.
2. The tournament detail route /torneos/[id] doesn't exist yet (T-017), so a successful create lands on a 404 until then.
