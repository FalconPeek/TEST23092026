RESULT: PASS

## Checks
- `npm run lint` → clean. `npm run typecheck` → pass. `npm test` → 257/257 pass. `npm run build` → success (/login is dynamic, /auth/auth-code-error is static).
- Files: all 6 declared files exist and nothing outside the task's list was needed.
- Security: every redirect target goes through `safeNextPath`. `next` is sanitized in the page and again in the actions/callback. No `getSession()` and no secret key. The provider is zod-validated. Errors return `fail(es...)` and never the raw Supabase error. `redirect()` is not wrapped in try/catch.
- Next 16: `searchParams` is awaited through `PageProps<"/login">`. `useActionState` is used correctly.
- Copy: everything comes from `es`, in voseo. The layout is mobile-first (max-w-sm, px-4).
- Test: it mocks the actions and covers both tabs, the OAuth buttons and the invalid email message.

## Notes (not failures)
- The magic-link flow was not tested end-to-end in a browser. The worker only checked the local Supabase ↔ Mailpit wiring over REST. Recommend a Playwright e2e test in a later task.
- In `signInWithOAuth`, `next` isn't zod-parsed, but it only feeds into `safeNextPath`, so it's safe.
- Consider enabling vitest `globals` (or a setup file) so RTL cleans up automatically, instead of adding `afterEach(cleanup)` in every test file.
