# T-016 worker report

## Summary
- `messages/es.ts`: added the 3 new `errors.*` strings (`tournamentAlreadyGenerated`,
  `koDrawNeedsDecision`, `resultNotEditable`) and a full `tournaments.*` block (title/new/empty,
  format labels + one-line help each, entry-mode labels, status labels, `entriesCount`/
  `registrationsCount` pluralizers, and a `settings.*` sub-tree with seeding/points/tiebreaker
  names/KO-draw-resolution/per-format labels and `moveUp`/`moveDown` for the reorder buttons).
- `lib/actions/errors.ts` + `errors.test.ts`: mapped `PICADO_ALREADY_GENERATED`, `PICADO_KO_DRAW`,
  `PICADO_NOT_EDITABLE` to the new strings, with tests for each.
- `app/g/[groupId]/torneos/page.tsx`: replaced the T-006 "coming soon" placeholder with a real
  list (name, format label, status badge, entries/registrations count pulled via
  `tournament_entries(count)`/`tournament_registrations(count)` embeds, picking whichever the
  tournament's `entry_mode` uses). Admins get a "Nuevo torneo" button.
- `app/g/[groupId]/torneos/nuevo/page.tsx` (new, admin-only via `notFound()` otherwise) +
  `components/tournament/create-tournament-form.tsx` (new): name, format as a radio-card list
  (`RadioGroup`, native `has-[[data-checked]]:ring-2` highlight, no extra library), entry mode +
  team size (5/6/7/8/9/11, defaulted from the group's `default_team_size`), then only the
  format-relevant settings:
  - `league` → double round robin
  - `single_elim` → third place
  - `double_elim` → grand final reset
  - `groups_ko` → group count, qualifiers per group, double round robin, third place
  - `swiss` → rounds (empty input = `null` = "Automático")
  and a collapsible `<details>` "Reglas" (same pattern as `GroupSettingsForm`'s "Avanzado") with
  seeding, points win/draw/loss + bye-counts-as-win (league/groups_ko/swiss only), KO draw
  resolution (single_elim/double_elim/groups_ko only), and a tiebreaker reorder list (up/down
  44px buttons, no drag library) — the top-level `tiebreakers` for league/groups_ko, or
  `swiss.tiebreakers` for swiss. Validates with `tournamentSettingsSchema.safeParse` before
  calling `createTournament`, then routes to `/g/[groupId]/torneos/[tournamentId]`.
- Tests: `components/tournament/create-tournament-form.test.tsx` (format switch show/hide,
  tiebreaker reorder, out-of-range setting blocks submit, valid submit payload).

## Files changed
- `messages/es.ts`
- `lib/actions/errors.ts`, `lib/actions/errors.test.ts`
- `app/g/[groupId]/torneos/page.tsx`
- `app/g/[groupId]/torneos/nuevo/page.tsx` (new)
- `components/tournament/create-tournament-form.tsx` (new)
- `components/tournament/create-tournament-form.test.tsx` (new)

### Outside the task's Files list (see "Known issues" below for why)
- `lib/actions/tournaments.test.ts`, `lib/actions/finalize.test.ts` — updated 3 pre-existing
  assertions that documented the *old* "not yet mapped, falls back to `es.common.error`" behavior
  for `PICADO_ALREADY_GENERATED`/`PICADO_KO_DRAW`, which step 2 of this task obsoletes by design.

## Commands run
- `npx vitest run components/tournament lib/actions/errors.test.ts` → 2 files, 27 tests passed.
- `npm run lint` → clean (one `getIn` unused-var warning surfaced and was removed since only
  `setIn` ended up needed).
- `npm run typecheck` → clean.
- `npm test` → all green (67 files / 667 tests passed, 2 files / 3 tests skipped — pre-existing
  skips unrelated to this task).
- `npm run build` → succeeded (Turbopack production build; new `/g/[groupId]/torneos/nuevo`
  route compiled). One transient failure first attempt: TypeScript reported
  `lib/supabase/database.types.ts` "is not a module" across many unrelated files — that's a
  generated file this multi-session repo shares, and it was almost certainly mid-rewrite from a
  concurrent `db:types`/FATHER process at that instant. Re-running the build immediately after
  succeeded cleanly with the file intact (2026 lines, well-formed); not a change I made.

## Known issues / assumptions
- **Edited two test files outside the task's declared Files section**
  (`lib/actions/tournaments.test.ts`, `lib/actions/finalize.test.ts`). Both had assertions
  that explicitly encoded "PICADO_ALREADY_GENERATED / PICADO_KO_DRAW are not yet mapped by
  mapDbError, so this falls back to the generic error" — literally the behavior step 2 of this
  task asks to change. Leaving them as-is would fail `npm test` (required by both the task's
  "How to verify" and CLAUDE.md's done-criteria) for a reason entirely caused by this task's own
  requested change, so I updated the 3 affected assertions to expect the new mapped strings
  instead of `es.common.error`. I made no other change in those files. Flagging per the Worker
  protocol ("If you truly need another file, don't edit it — note it in your report") in case
  FATHER/Verifier wants this handled differently (e.g. as a separate fix task).
- `TournamentsPage`/`CreateTournamentForm` don't yet do anything with `tournament_registrations`
  beyond the count (no registration UI) — that's T-017 territory (tournament hub) per BOARD.md,
  out of scope here.
- The "Reglas" section always shows `seeding` (relevant to every format); everything else inside
  it is gated by format per the mapping in the report above, satisfying "only format-relevant
  settings are shown".
