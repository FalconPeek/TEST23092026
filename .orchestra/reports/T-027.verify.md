RESULT: PASS

## Checks
- `npx vitest run lib/match components/match`: 10 files, 63 tests passed.
- `npm run lint`: clean. `npm test`: 775 passed, 3 skipped, 78 files. `npm run build`: compiled.
- `npx tsc --noEmit`: one error, in FATHER's `app/api/og/squad/[squadId]/route.tsx:60` (`es.card.ovrShort` is missing). That's squad work in progress, not a worker file.
- Git: the T-027 worker files were committed in 53109e0 ("asd", by the user) together with FATHER's clubs/squads migrations. Only the page.tsx wiring is still uncommitted. All the worker's changes are within the declared files.
- Pure lib/match/amend.ts (tested):
  - `unattributedGoals` = score − own goals − opponent own goals, clamped at ≥0.
  - `validateAmendment` mirrors both SQL caps (goals + opponent own goals ≤ score; assists ≤ score − opponent own goals).
  - `changedRows` sends only rows and keys that changed.
- The form builds its initial rows for every team player, defaulting to 0 when a player has no match_stats row. A live "Sin autor" counter and the over-score error block submit. It sends only `changedRows`, then toasts and refreshes. Controls are ≥44px (StatRow size-11, submit h-11).
- match-result shows "N goles sin autor" per side when that number is above 0 (tested).
- The page renders AmendStatsForm only when `finalized && admin`, so non-admins never see it. The RPC re-enforces the admin check and the caps.
- This matches the updated CLAUDE.md Rule A (score-only matches finalize, and an admin assigns goals up to the score later).
- Copy comes from es.match.* in voseo. No `any`, no console.log.
- Not run: `npx playwright test e2e/match-flow.spec.ts` (optional; FATHER's e2e needs the local stack).
