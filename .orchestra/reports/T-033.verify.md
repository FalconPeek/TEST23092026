RESULT: PASS

## Checks
- `npx vitest run components/tournament components/clubs components/squads`: 8 files, 53 tests passed.
- `npm run lint`: clean. `npx tsc --noEmit`: clean. `npm test`: 876 passed, 3 skipped, 91 files. `npm run build`: succeeded.
- Changed files are all declared in the task.

## Step by step
1. The match-admin-menu steppers now have `es.match.decrease/increase(label)` aria-labels, and the test finds them by name.
2. The format card ring uses `has-[[data-state=checked]]`. Radix sets `data-state`, and the test asserts both the class and the item's actual `data-state="checked"`. The earlier `data-checked` selector was dead.
3. Crest cleanup: when a new upload uses a different extension, the old object is removed after the save succeeds, and "Quitar escudo" removes the object. Both are best effort (`.catch(() => {})`) and never block. Tested.
4. The squad picker empty state now uses `squads.noCandidates` (tested).
5. The plantillas/** and clubes/** RSC pages now `throw` on list-query errors instead of falling back to `[]`. The maybeSingle → notFound path is unchanged.
- Live REST check (stack up, as demo01), using the exact embed strings from these pages:
  - squads with `squad_slots(...)`: OK.
  - squads with `owner:players!squads_owner_player_id_fkey(display_name), squad_slots, squad_likes(count)` and published=true: OK. It returns "Los Galácticos" with the owner name, which confirms FATHER's 80b2c82 fix.
  - clubs with `club_players(count)`: OK.
  - club_players with `players(id, display_name, avatar_url)`: OK.
- No component still renders an unlabeled −/+ button.
