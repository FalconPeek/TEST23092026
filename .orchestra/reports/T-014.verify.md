RESULT: FAIL

## Checks
- `npx vitest run lib/match components/match`: 4 files, 28 tests passed.
- `npm run typecheck`: passed. `npm test`: 592 passed, 3 skipped, 59 files.
- `npm run lint`: 0 errors, 3 warnings, all in FATHER files (lib/actions/tournaments.ts, lib/server/badges-repo.ts).
- `npm run build`: fails with "Can't resolve '@/lib/server/tournament-advance'", imported from FATHER's lib/actions/tournaments.ts and lib/server/finalize-repo.ts. That's not a worker file, so the build couldn't confirm the worker route; the typecheck covers it.
- Changed files are all declared in the task.
- Privacy is OK:
  - The page selects score_reports, stat_reports and match_ratings filtered by `reporter_player_id`/`rater_player_id` = my player (RLS is also own-only).
  - Other reporters appear only as counts via get_match_report_summary.
- Eligibility gating is OK:
  - The score/stats forms show only for a team player while `reporting` and before report_deadline.
  - The rating form shows only for a player, or a spectator when spectators_can_rate, during reporting/pending_finalize and before rating_deadline. A clear message is shown otherwise.
- Pure report.ts: stat rows are sent only when non-zero or previously reported, goalsCheck is warning-only, and ratingsComplete works as specified. Rating-form tests cover not submitting until all are rated, max 2 standouts, and self excluded.
- Score steppers (size-11 = 44px) and rating buttons (h-11) meet the tap-target size.

## Problems
1. `components/match/stats-form.tsx:51` and `:60`: the stat stepper −/+ buttons are `size-6` (24px). The acceptance criterion says steppers must be ≥44px tap targets. Expected at least `size-11` (or `min-h-11 min-w-11`, with the row layout adjusted so 4 steppers still fit at 375px, e.g. a 2×2 grid per player).
2. (Minor, same criterion) `components/match/rating-form.tsx:98`: the standout search input is `h-8` (32px). Should be h-11 for consistency with the ≥44px tap-target requirement.
