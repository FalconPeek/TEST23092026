RESULT: PASS

## Checks
- `npx vitest run lib/match lib/format.test.ts components/match`: 3 files, 38 tests passed.
- `npm run lint`: clean. `npm run typecheck`: passed. `npm test`: 507 passed, 3 skipped, 52 files. `npm run build`: succeeded, and the 3 match routes are dynamic.
- Changed files are all declared in the task.
- Admin-only pages: nuevo and equipos call notFound() for a non-admin or a missing membership. Equipos also calls notFound() for a match from another group or one not in `scheduled`. The list page shows "Nuevo partido" only to admins.
- Lineup editor:
  - The balance is local state only; nothing is saved until "Guardar equipos", which calls setMatchLineup.
  - After start/cancel/save it navigates to /partidos, so it never lands on a 404 for a non-scheduled match.
  - mu defaults to 25, ovr to 60, and isGk is primary position POR.
- Spectator-role members only cycle between unassigned and spectator. The pure lib/match/lineup.ts tests cover the cycle order, the spectator restriction, balancing the selected players only, and the position being kept.
- toArgentinaIso uses a fixed -03:00 (Argentina has no DST), and display goes through the lib/format es-AR/Buenos_Aires helpers, so times don't depend on the browser's timezone. It's tested.
- The list fetches match_results and teams through RLS. Recent matches are limited to 30.
- Copy comes from es.matches.* and es.matches.status.* in voseo. No `any`, no console.log.

## Notes (non-blocking)
- `cancelConfirm` is reused for the "Terminó el partido" confirm dialog.
- In the lineup page, the pre-balance openskill/card reads are fine (RLS select), but they add 2 extra queries. Acceptable.
