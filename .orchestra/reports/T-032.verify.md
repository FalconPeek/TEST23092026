RESULT: PASS

## Checks
- `npx vitest run components/tournament lib/tournament`: 7 files, 69 tests passed.
- `npm run lint`: clean. `npx tsc --noEmit`: clean. `npm test`: 870 passed, 3 skipped, 91 files. `npm run build`: succeeded.
- Live check (the local stack is up), signed in as demo01 through the REST API with a session token:
  - The new `clubs?select=…,club_players(player_id)` embed returns rows with rosters, and isn't ambiguous (single FK).
  - `tournament_entries?select=id,name,seed,player_ids,club_id` returns without error, so the new column is valid.
  - The other new queries are plain selects, with no embeds.
- `setEntryClub` (pure, tested) sets the name, clubId and the roster minus players already in other entries, so no player ends up in two entries. Clearing the club keeps the name and players. The payload sends `clubId`.
- ClubCrest (sm) renders in the hub list, bracket-view, standings-table (render test) and fixtures-list. Each page loads the group's clubs in one query and passes the map down.
- Copy is es.tournament.useClub/noClub. No `any`, no console.log.

## Notes
- `components/tournament/fixtures-list.test.tsx` was modified (new `clubsById` prop in fixtures) but isn't in the Files list. It's a trivial adaptation, so I'm accepting it.
