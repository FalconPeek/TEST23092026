RESULT: PASS

## Checks
- `npx vitest run lib/rankings components/rankings`: 2 files, 14 tests passed.
- `npm run lint`: clean. `npx tsc --noEmit`: clean. `npm test`: 806 passed, 3 skipped, 82 files. `npm run build`: succeeded, and /rankings is dynamic.
- Changed files are all declared in the task.
- `parseMetric` whitelists the 8 metrics and falls back to `ovr`, handling arrays and undefined. Only the parsed value reaches the RPC.
- The RPC is called in the RSC with the session client, and the page calls notFound() on an error such as PICADO_NOT_MEMBER. My own row is identified by my active player in the group.
- The podium handles ties (a position can hold multiple rows, tested). Rank 4+ goes to LeaderboardList. Players link to their profiles.
- 375px: only the chip bar scrolls (`-mx-4 overflow-x-auto px-4`), and the chips are min-h-11. Nothing else has fixed widths.
- Help text shows for impacto and avg_rating. Copy comes from es.rankings.* in voseo. No `any`, no console.log.

## Notes (non-blocking)
- `formatMetricValue` uses `toFixed(1)`, which gives "7.5". es-AR would be "7,5", and lib/format.ts already has `formatDecimal` (used on the profile page). Worth switching for consistency.
