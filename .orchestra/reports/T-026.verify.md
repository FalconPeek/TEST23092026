RESULT: PASS

## Checks
- `npx vitest run lib/notifications components/notifications components/app-shell`: 5 files, 32 tests passed.
- `npm run lint`: clean. `npx tsc --noEmit`: clean. `npm test`: 838 passed, 3 skipped, 87 files. `npm run build`: succeeded, and /notificaciones and /yo/ajustes are dynamic.
- safeInternalUrl:
  - It requires a leading `/`, rejects `//`, control characters and backslashes, then parses against a sentinel origin and checks that the origin matches.
  - It returns null (no link) for anything unsafe, and it's tested with //evil.com, https:// and javascript:.
  - Both the list page and the bell toast action use it, so links can never navigate off-site.
- The bell subscribes once per userId in useEffect and calls `removeChannel` in cleanup, so there are no duplicate subscriptions (deps [userId]). The initial count comes from the server with `head: true`, and inserts increment it and show a toast.
- /notificaciones shows my last 50 through RLS (own rows only). Unread items are highlighted, "mark all read" goes through the action, and opening an item marks it read.
- The prefs form toggles with merged prefs and handles the push states (unsupported/denied/enabled/disabled) with Spanish copy (tested).
- Copy comes from es.notificationsUi.* in voseo. No `any`, no console.log.

## Notes
- `app/yo/page.tsx` was edited (a "Ajustes" link to /yo/ajustes) but isn't in the Files list. Step 4 explicitly asks for a link "from /yo", so I'm accepting it. FATHER, please include it in the commit.
- The list falls back to the raw `n.kind` when a payload has no title. That's unlikely (every template sets one), but `es.notificationsUi.<kind>` would be a nicer fallback.
