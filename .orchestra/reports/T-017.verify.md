RESULT: PASS

## Checks
- `npx vitest run lib/tournament components/tournament`: 3 files, 30 tests passed.
- `npm run lint`: clean. `npx tsc --noEmit`: clean. `npm test`: 702 passed, 3 skipped, 69 files. `npm run build`: succeeded, and /torneos/[tournamentId] is dynamic.
- Layout: a tournament that isn't visible or belongs to another group gets notFound(). The header shows name, status, format, entry mode and team size. Tabs are gated by format and status as specified.
- Admin gating:
  - `TournamentAdminControls` renders only when `admin`, following the status flow (draft → open; draft/registration → generate with confirm, disabled plus needTwoEntries below the threshold; in_progress → finish).
  - EntriesEditor is editable only when admin and in draft/registration; everyone else gets a read-only list.
- Spectators never see "Anotarme": the button is gated on `myRole !== "spectator"`, status `registration`, and having a player row.
- No bracket or state is computed client-side; generation goes only through `generateTournamentBracket`. The pure lib/tournament/entries.ts covers the ≤1 team per player rule by construction plus validation (too_few, empty_name, duplicate_seed), and it's tested.
- Copy comes from es.tournament.* in voseo. No `any`, no console.log.

## Files
- Extra file: `components/tournament/tournament-hub-client.tsx` (new) is outside the declared Files list. The worker flagged it transparently, and it's needed: the layout and page are Server Components, so the interactive tabs and buttons need a client module. No other task declares this file. I'm accepting it as a note, not a failure. FATHER, please adopt it into the T-017 commit.

## Notes (non-blocking)
- The /llave, /tabla and /fechas tabs 404 until T-018/T-019 land.
- "Abrir inscripción" and "Terminar torneo" give no success toast; only a refresh shows the change. That's fine per the spec.
