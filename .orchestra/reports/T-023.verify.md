RESULT: PASS

## Checks
- `npx vitest run lib/notifications components/player lib/server`: 113 passed, 3 skipped.
- `npm run lint`: clean. `npx tsc --noEmit`: clean. `npm test`: 792 passed, 3 skipped, 80 files. `npm run build`: succeeded.
- Changed files are within the declared list. No lib/server change was needed because the badge builder takes `badgeCode`, and its callers in card-badges, finalize-repo and match-notify already pass codes.
- `FALLBACK_STRINGS` and the defensive shape check are gone. Every template reads `es.notifications.<kind>(params)`.
- `badgeAwardedPayload` resolves `es.badges[code].name`. It falls back to the raw code only for a code missing from the catalog, which can't happen for the 15 seeded codes (all present in es.badges).
- es.ts has all 9 notification kinds, all 15 badges (assist_king is "3+ asistencias en un partido"), badges.title/count/none, and profile.share/downloadCard/shareText, all in voseo.
- BadgeList: icon map with a fallback, ×n for count > 1, and an empty state (tested). The profile loads `player_badges` with the `badges(icon, sort)` embed through the RLS session client.
- ShareCardButton: tries a file share when `canShare({files})`, then a URL share, then a clipboard copy with a toast, and ignores AbortError. There's also a "Descargar imagen" link with a `download` attribute (all tested).
- No `any`, no console.log.

## Notes
- templates.ts:111 uses `as unknown as Record<...>` to index es.badges by a string code. A typed `isBadgeCode` guard would be cleaner, but it's acceptable.
