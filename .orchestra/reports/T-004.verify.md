RESULT: PASS

## Checks
- `npx vitest run components/card` → 5/5 pass. `npm run lint` → clean. `npm run typecheck` → pass. `npm test` → 262/262 pass. `npm run build` → success.
- Production 404: the built `.next/server/app/dev/cards.html` is the not-found shell and contains no sample card data. The `notFound()` guard is the first statement.
- Files: the task only added components/card/* and app/dev/cards/page.tsx. Other working-tree changes (migrations, db tests, database.types.ts, CLAUDE.md) come from FATHER or other tasks.
- Review: all prop types are exported exactly as specified. `PositionCode = keyof typeof es.positions`. No `any`, no hooks, no 'use client'. The root is a `<figure>` with `aria-label="name, ovr positionName"`, and stats are real text. Labels come from `es.card.faceStats`/`gkStats`. PlayStyles are capped at 3 with a ring for the `plus` variants. Provisional uses the grey palette plus a badge with a hint title, and the real tier stays hidden. The special tier's sheen is disabled via `motion-reduce:hidden`. Tier classes are static literals, so Tailwind picks them up. `cn` comes from the project's `cn` package, the same as components/ui.

## Notes (not failures)
- No visual check at 375px (no browser available, and proxy.ts requires sign-in for /dev/cards). FATHER may want /dev/* public in development, or a seeded E2E user.
- The sheen keyframes list `100%` twice (`0%, 100%` and `60%, 100%`). The last one wins, so it works, but `0%, 100%` should just be `0%`.
- The `preferredFoot` prop is accepted but not rendered. The spec doesn't require it on the card face.
- The provisional badge uses a fixed `text-[9px]` at every size.
