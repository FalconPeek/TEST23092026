RESULT: FAIL

## Checks (T-021 checked against all of T-014's criteria)
- `npx vitest run lib/match components/match`: 5 files, 34 tests passed, including the new stats-form.test.tsx.
- `npm run lint`: 0 errors. `npm run typecheck`: passed. `npm test`: 602 passed, 3 skipped, 60 files. `npm run build`: succeeded, and /g/[groupId]/partidos/[matchId] is dynamic.
- Fixed:
  - The stats steppers are now size-11 in a 2×2 grid per player. That fits at 375px: each column is about 160px and a stepper is about 44+44+value.
  - The standout search input is now h-11. Score steppers (size-11) and rating segments (h-11) were already OK.
- The rest of T-014 still holds:
  - Only my own reports and ratings are read (filtered by my player id); others appear only as counts via get_match_report_summary.
  - Gating by eligibility and deadlines is intact.
  - report.ts and the rating-form behavior are unchanged and tested.
- Changed files are within T-021/T-014's declared list. The lib/actions/matches* changes are FATHER's, per FATHER's note.

## Problems (the T-021 criterion: "every stepper, rating button and input in the match detail forms ≥44px")
1. `components/match/rating-form.tsx`, the standout chip `<button>` in the `filtered.map` (className `rounded-full border px-2 py-0.5 text-xs`): about 22px tall. Needs `min-h-11` (with px-3), or a list of rows at h-11.
2. `components/match/stats-form.tsx:121-124`, the "mostrar atajadas" `<input type="checkbox">` inside a text-xs label: its tap area is about 16–20px. Replace it with a shadcn Switch/Checkbox wrapped in a label that has `min-h-11` (e.g. `className="flex min-h-11 items-center gap-2"`).
