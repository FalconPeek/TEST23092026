RESULT: PASS

## Checks
- `npx vitest run components/scouting`: 2 files, 11 tests passed.
- `npm run lint`: clean. `npm run typecheck`: passed. `npm test`: 489 passed, 3 skipped, 50 files. `npm run build`: succeeded, and /votar is dynamic.
- Changed files are messages/es.ts, components/scouting/* and the votar page, all declared in the task.
- Vote page:
  - RSC using the session client; a player who doesn't exist or belongs to another group gets notFound().
  - Status comes from `get_scouting_status`, and the prefill from `get_my_scouting_ballot`, which returns only my own rows. No other user's votes are fetched.
  - The initial tab follows the mode of my latest ballot.
- isGk is derived from the primary or alternate positions including POR:
  - The GK attribute group and the GK playstyles are only offered when isGk (tested).
  - The SQL enforces the same thing server-side.
- Submit rules:
  - Quick mode always sends all 6 keys (default or prefilled values).
  - Detailed mode sends only touched keys, and its submit is disabled with zero touched keys (tested).
  - Playstyles are capped at 5 (tested). The star save is disabled until a star is touched.
- `!canVote` shows the reason in an Alert and disables inputs (tested). The cooldown reason uses formatDate from lib/format (no circular import: lib/format has no imports).
- 375px layout: the save bar is `sticky bottom-20`, above the bottom nav, and the sliders are full-width.
- Copy comes from es.scouting.* in voseo (plus `unset` "Sin votar"). No `any`, no console.log.

## Notes (non-blocking)
1. In quick mode, untouched sliders submit the neutral default of 5 for every face stat. That's what the spec says, but it may bias ratings toward 5 when a user only adjusts one or two. Consider requiring each slider to be touched (or confirmed) in quick mode.
2. Per-test ResizeObserver/hasPointerCapture polyfills are repeated in each test file. A shared vitest.setup polyfill would help (FATHER's call).
