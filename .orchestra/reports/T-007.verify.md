RESULT: PASS

## Checks
- `npm run lint`: clean. `npm run typecheck`: passed. `npm test`: 315/315 passed (34 files, including the 2 new join-button tests). `npm run build`: succeeded, and /invitacion/[code] is dynamic.
- Changed files are messages/es.ts, app/invitacion/[code]/page.tsx and components/invite/*, all declared in the task. FATHER's untracked finalize/tournament files were ignored.
- Code format is checked with `^[A-Za-z0-9_-]{6,32}$` before anything else. A bad code shows the invalid state and makes no DB call.
- Unauthenticated visitors never trigger an RPC: the getUserId check comes before createClient/rpc. The login link is `/login?next=%2Finvitacion%2F<code>`, and login uses safeNextPath, then the callback carries `next` through, so the user lands back on the invite.
- The RPC error (an unknown code raises PICADO_INVITE_INVALID), a missing row, and `valid=false` all show the InvalidState component, never an error page. The worker was right that the RPC raises an error rather than returning zero rows; the page handles both cases.
- JoinButton calls acceptInvite (a zod-validated server action), has a pending state, uses router.replace and toasts on success, and toasts the already-mapped error on failure.
- Copy comes from es.invite.* and uses voseo. Layout is mobile-first and centered.
- No `any` and no console.log.

## Notes
- The invalid state's "Ir a mis grupos" button goes to /g, which sends a signed-out user to login. That's fine, since signed-out users only see the invalid state when the code format is bad.
