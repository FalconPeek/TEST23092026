RESULT: PASS

## Checks
- `npx vitest run components/squads`: 5 tests passed, including the lineup-mode additions (excluded players hidden; payload includes matchId and side).
- `npm run lint`: clean. `npx tsc --noEmit`: clean. `npm test`: 861 passed, 3 skipped, 91 files. `npm run build`: succeeded.
- Cancha mode lives on the equipos page, which already calls notFound() for non-admins and for non-`scheduled` matches (T-013), so only admins on scheduled matches see it.
- Cross-side exclusion is live: each side's unsaved assignment is lifted into lineup-pitch-editors and passed as `excludePlayerIds` to the other side.
- "Usar estas alineaciones" stays disabled (with the needBothSides hint) until both lineup squads exist. Saving in lineup mode refreshes the page, so the server-computed `bothSaved` updates. There's a confirm dialog before `applyLineupSquads`, and slot positions and club naming are handled server-side by the action/RPC.
- SquadEditor's dream behavior is unchanged; the lineup props are additive.
- The match header shows ClubCrest only when `club_id` is set, via one extra `clubs` query (RLS) only when needed.
- Copy comes from es.matches.* in voseo. No `any`, no console.log.

## Notes
- The extra file `components/squads/lineup-pitch-editors.tsx` is outside the Files list and wasn't flagged. It's the client wrapper that coordinates both sides, and it's needed. FATHER, please include it in the commit.
- I didn't run `npx playwright test e2e/match-flow.spec.ts` because it needs the local stack and dev server. The API lineup path isn't touched by this diff.
