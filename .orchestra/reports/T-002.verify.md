RESULT: PASS

## Checks
- `eslint` on app/ and components/theme-provider.tsx → clean. `npm run typecheck` → pass. `npm test` → 230/230 pass. `npm run build` → success (`/` is static).
- Files: the task's changes are limited to app/globals.css, app/layout.tsx, app/page.tsx and components/theme-provider.tsx. The other modified or untracked files (lib/brackets, supabase/migrations, .orchestra, debug4.test.ts) belong to other sessions.
- layout: `lang="es-AR"`, `dark` class, suppressHydrationWarning; title default "Picado" plus template "%s · Picado"; description = es.app.tagline; applicationName is set. The viewport export sets themeColor, device-width, initialScale 1 and viewportFit cover. ThemeProvider (dark, no system theme) wraps the Toaster (top-center, richColors). Body uses min-h-dvh with safe-area padding.
- globals.css: tier tokens (from/to/fg/accent) exist for all 5 tiers as --tier-* and as --color-tier-* inside @theme inline. `@utility fut-shield` has a clip-path polygon with cut top corners and a pointed bottom.
- page.tsx: Server Component. All copy comes from `es`. The CTA is a Button asChild wrapping a Link to /login. Layout is mobile-first (single column, sm:2 columns, max-w). No hard-coded strings.

## Notes (not failures)
- /login 404s until the login task lands. That's expected.
- The root `debug*.test.ts` files (another session's) should be removed before M1 closes.
