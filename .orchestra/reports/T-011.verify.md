RESULT: PASS

## Checks
- `npx vitest run lib/cards components/player`: 3 files, 17 tests passed.
- `npm run lint`: clean. `npm run typecheck`: passed. `npm test`: 478 passed, 3 skipped, 48 files. `npm run build`: succeeded, and /g/[groupId]/jugadores/[playerId] is dynamic.
- Changed files are all declared in the task (FATHER's tournament/finalize files were ignored).
- toCardProps is pure. It validates face jsonb against the exact outfield or GK shape, playstyles against `{code, plus}[]` filtered through isPlayStyleCode, and position/tier against POSITIONS/TIERS. Stars are clamped 1–5 with a default of 3. It returns null for a missing row or malformed jsonb, and all of this is tested.
- Profile page:
  - RSC only, using the session client (RLS); there are no client-side Supabase reads. The radar gets its data as props.
  - A player who doesn't exist or belongs to another group gets notFound().
  - With no card it shows an empty state and skips the radar, chips and attributes. With no stats it shows an empty state.
  - Votar is hidden on my own player; Editar perfil shows only on my own player.
- AttributeList groups attributes under the 6 face stats, adds the GK group only for GK, and uses bar colors at the 50/65/75 thresholds (tested). The radar has an sr-only list of values and a 0–99 domain.
- Group home avatars now link to the player profile.
- es.playstyles has 30 labels and es.profile.* uses voseo. The layout is mobile-first (grid-cols-3 stat tiles, flex-wrap chips).
- No `any`, no console.log.

## Notes (non-blocking)
1. The "Media por posición" chips show the raw position code (e.g. "DC 78") instead of `es.positions[code]`. That's acceptable as an abbreviation; consider it for the polish task.
2. Bar colors use the Tailwind palette (red/amber/lime/green) rather than design tokens.
3. The "Votar" link points to /votar, which T-012 will create.
4. The empty stats state reuses `es.groups.noMatches` ("No hay partidos programados."), which reads oddly for past stats. Consider a `profile.noStats` key.
