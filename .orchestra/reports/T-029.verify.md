RESULT: PASS

## Checks
- `npx vitest run components/clubs`: 2 files, 8 tests passed.
- `npm run lint`: clean. `npx tsc --noEmit`: clean. `npm test`: 846 passed, 3 skipped, 89 files. `npm run build`: succeeded, and /clubes and /clubes/[clubId] are dynamic.
- Crest validation happens before upload: the MIME whitelist is png, jpeg and webp (SVG isn't on it), and the size cap is 512 KB. A failure returns early with a Spanish toast before any storage call (tested). The upload path is `<groupId>/<clubId>.<ext>` with upsert, followed by `updateClub({crestPath})`, and the public URL gets a cache-bust.
- Admin gating: "Nuevo club" and the edit form only render for admins; non-admins get the read-only view. Storage RLS and the RPCs enforce this too.
- ClubCrest renders the image, or falls back to a shield with the primary color and the short name (tested).
- Shirt numbers are unique and 1–99 (tested).
- Controls are ≥44px. Roster rows are min-h-11 labels wrapping the size-6 checkbox, so the tap area is 44px.
- There are entry links on the group home and in ajustes. Copy comes from es.clubs.* in voseo. No `any`, no console.log.

## Notes
- The extra file `components/clubs/create-club-dialog.tsx` is outside the Files list and wasn't flagged in the worker report. It's needed for the "Nuevo club" button, so I'm accepting it. FATHER, please include it in the commit.
- Replacing a crest with a different extension (png → jpg) leaves the old object orphaned in storage. Minor; consider removing the old path on replace.
