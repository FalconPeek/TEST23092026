RESULT: PASS

## Checks
- `npx vitest run lib/actions lib/rating/playstyles.test.ts`: 6 files, 90 tests passed.
- `npm run lint`: clean. `npm test`: 420 passed, 1 skipped, 44 files.
- `npm run typecheck`: the first run failed only in FATHER's untracked `lib/server/tournament-repo.ts`, which isn't a worker file. The rerun was clean.
- Changed files are all declared in the task. The index.ts change is exactly one re-export line.
- Payload JSON keys match the SQL (`->>`/`->` usages in the migrations): `player_id`, `position`, `name`, `color`, `subject_player_id`, `goals`, `assists`, `own_goals`, `saves`, `target_player_id`, `rating`, `standout_attributes`.
- All 7 new PICADO_* prefixes exist in the migrations and map to the specified voseo strings, and errors.test.ts is extended.
- Every action follows zod safeParse → getUserId → session client rpc → mapDbError → revalidatePath → ActionResult. No admin client (only inside recomputeNow), no `any`, no console.log.
- Recompute runs only after a successful vote, inside try/catch with the explanatory comment. Tests cover both the not-called-on-failure and still-ok-on-throw cases.
- Zod constraints:
  - Scouting uses `z.partialRecord` over OUTFIELD_FACE_STATS / ALL_ATTRIBUTES with ≥1 vote of integers 1–10.
  - Playstyles use the enum with ≤5 unique codes.
  - Stars are 1–5 with at least one given; null is passed when absent, which the SQL handles.
  - teamSize is in {5,6,7,8,9,11}.
  - The lineup has no duplicate across the two teams and the spectators.
  - Stats are 0–30 (saves 0–99) with unique subjects.
  - Ratings are 1–10 with ≤2 standout attributes and unique targets.
- PLAYSTYLES has the 30 codes in the specified order, the GK subset is the last 5, and it's tested.

## Notes
- Standout attributes accept ALL_ATTRIBUTES (including gk_*), which matches the SQL `private.sub_attribute_keys()` constraint.
