RESULT: PASS

## Checks
- `npm run lint`: clean. `npm run typecheck`: passed. `npm run build`: succeeded, and /g/[groupId]/ajustes is dynamic.
- `npm test`: 354 passed, 1 skipped. My first run hit a FAIL on a transient root file `cleanup-check.test.ts` from another session; that file was gone moments later and wasn't the worker's. On rerun, 38 files passed and none failed.
- Changed files are all declared in the task. FATHER's finalize/api/migration files were ignored.
- Checked `lib/permissions.ts` against the SQL in set_member_role, remove_member, create_invite and claim_guest_player:
  - The owner can make/unmake admins and remove anyone except the owner.
  - An admin can toggle member↔spectator and remove members/spectators, but gets no actions on other admins.
  - Members and spectators get no actions. A target who is the owner, or a self-target, gets no actions.
  - Admin invites are owner-only, and claiming a guest is allowed for owner/admin/member.
  - Conclusion: the UI never offers an action the SQL would reject. Unit and component tests cover the required role pairs.
- Guests are filtered by `is_guest`. Claim and assign set `is_guest=false` in SQL, so claimed guests correctly drop out of the list.
- Invites are fetched only when `isGroupAdmin` (RLS also restricts them). Status is derived from revoked/expired/usedUp/active, in that order. The invite URL comes from `siteUrl()` (NEXT_PUBLIC_SITE_URL) and is copied with navigator.clipboard.
- Danger zone: leave is disabled with a hint for the owner, and leaving goes through router.replace('/g'). Transfer is owner-only with a member select.
- The T-006 follow-up (step 4) is fixed: /g counts `group_members` rows, and the group home uses a count with `head: true`.
- Copy comes from es.settings.* and uses voseo. Layout is mobile-first (single column, truncation, shrink-0 action buttons).
- No `any`, no console.log, no admin client.
- I did no manual two-user flow because there's no browser or E2E user.

## Notes
- The transfer target list includes spectators. That's allowed by SQL (any member).
