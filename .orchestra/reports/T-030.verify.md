RESULT: PASS

## Checks
- `npx vitest run lib/squads components/squads`: 4 files, 30 tests passed.
- `npm run lint`: clean. `npx tsc --noEmit`: clean. `npm test`: 859 passed, 3 skipped, 91 files. `npm run build`: succeeded, and /plantillas, /nueva and /[squadId] are dynamic.
- Rating and chemistry: every display (editor, list, featured card, read-only pitch) goes through `buildSquadView` from lib/squads. The only math in the components is clamping chemistry dots to 0–3 for display.
- No one else's unpublished squads are fetched:
  - "Mis plantillas" is filtered by `owner_player_id = my player`.
  - "Del grupo" is filtered by `published = true`, and my own squads are excluded client-side.
  - The featured squad comes from the RPC's id, and RLS still applies.
  - /[squadId] relies on RLS plus an explicit `if (!squad.published) notFound()` for non-owners.
- 375px: the pitch is `aspect-[4/5] w-full max-w-[420px]` with markers at size-11 (44px). The player picker is a shadcn Sheet with `side="bottom"`.
- Share uses the /api/og/squad PNG as a file when canShare allows, else the URL, else the clipboard. Like toggling happens only on others' squads.
- The editor-state helpers (set/clear slot, keep valid slots on a formation change, candidates sorted by slot OVR) are tested. The editor test covers the rating/chemistry update, no double placement, and a save payload without positions.
- Copy comes from es.squads.* in voseo. No `any`, no console.log.

## Notes
- The extra file `components/squads/like-button.tsx` is outside the Files list and wasn't flagged. It's needed for the like toggle, so I'm accepting it. FATHER, please include it in the commit.
- The share logic is duplicated from share-card-button (T-023) rather than extracted into a shared helper. Minor.
