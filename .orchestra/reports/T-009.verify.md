RESULT: PASS

## Checks
- `npm run lint`: clean. `npm run typecheck`: passed. `npm test`: 370 passed, 1 skipped, 41 files, no failures. `npm run build`: succeeded, and /g/[groupId]/jugadores/[playerId]/editar is dynamic.
- Changed files are all declared in the task (FATHER's untracked files were ignored).
- Group settings form:
  - Its state is the full parsed GroupSettings, and it runs `groupSettingsSchema.safeParse` before calling updateGroup, so invalid values never reach the action. The server re-parses as well (T-005).
  - Every common field on the list is present.
  - "Avanzado" walks `settings.rating` recursively and renders every numeric leaf except the two common ones, so new tunables appear automatically.
  - Reset goes back to `defaultGroupSettings`.
  - The section is admin-only, and the page reads settings via `parseGroupSettings`. Tests cover defaults, invalid-blocks-submit and reset.
- Editor page:
  - A player from another group, a player who has left, or a player that's neither mine nor a guest I'm admin of all get notFound(). Guests edited by an admin go through `updatePlayer`; my own profile goes through `updateMyPlayer`.
  - The form offers the 15 positions and at most 4 alternates, excludes the primary, and drops the primary from the alternates when it changes. Height is 120–230, and the zod schema in the action re-enforces it.
  - Tests cover max 4 and primary excluded.
- "Editar mi perfil" link on the group home: shown only for my own player.
- Copy comes from es.groupSettings.* and es.player.*, in voseo. The layout is single-column and becomes 2 columns at sm.
- No `any`, no console.log, no admin client.
- I did no manual round-trip because there's no browser or E2E user.

## Notes (non-blocking)
1. In the settings form, clearing a number input gives `Number("") = 0`. For fields whose schema allows 0, a cleared field silently becomes 0 instead of showing an error.
2. In the profile form, clearing height or foot sends `undefined`, so an existing value can't be cleared (the RPC likely keeps the old value).
3. Advanced labels are humanized English keys, as the task specifies. That isn't Spanish copy, which CLAUDE.md generally requires, so consider `es.groupSettings.advanced.*` labels later.
