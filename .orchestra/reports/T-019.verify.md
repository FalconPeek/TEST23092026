RESULT: PASS

## Checks
- `npx vitest run components/tournament`: 5 files, 29 tests passed (standings-table and fixtures-list are new).
- `npm run lint`: clean. `npx tsc --noEmit`: clean. `npm test`: 732 passed, 3 skipped, 73 files. `npm run build`: succeeded, and /tabla and /fechas are dynamic.
- Changed files are all declared in the task.
- Standings come only from `computeStandings(createSupabaseTournamentRepo(session client), id, rng)`. The optional `rng` parameter exists in the signature, and a deterministic per-tournament seed keeps "lots" stable across refreshes, which is a sensible choice. The components do no points or ranking math; they only display.
- groups_ko qualification is `rank <= settings.groups_ko.qualifiers_per_group`, with a left accent bar. The swiss Buch./S-B columns show only for swiss, and there's a lot marker. All of this is tested.
- 375px: by default only Pos, Equipo, PJ, DG and Pts show, and the extra columns are `hidden sm:table-cell`. The shadcn Table sits in its own scroll container, so there's no page-level horizontal scroll.
- Fixtures are grouped by matchday, swiss round, or group then round, and pure-KO formats fall back to bracket sections. Byes are handled (tested). MatchAdminMenu appears only when `admin` (from the group role), and realtime from T-018 is reused on both pages.
- Tabla calls notFound() for single/double elim, and both pages call it for another group or an invisible tournament.
- Copy comes from es.standings.* in voseo. No `any`, no console.log.

## Notes (non-blocking)
- The Fechas tab for pure-KO formats duplicates the Llave view in list form. That's fine; whether to hide it is FATHER's call (worker flagged).
- Bye/TBD name resolution is duplicated between fixtures-list.tsx and bracket-layout.ts. It's small; consider extracting it later.
