RESULT: PASS

## Checks
- `npx vitest run lib/dashboard components/charts`: 2 files, 10 tests passed.
- `npm run lint`: clean. `npx tsc --noEmit`: clean. `npm test`: 816 passed, 3 skipped, 84 files. `npm run build`: succeeded, and /yo is dynamic.
- Changed files are all declared in the task.
- `?group=` is accepted only if it matches one of my `group_members` rows; otherwise it falls back to the first group, so it can't show a group I'm not in. The RPC also enforces membership.
- The RPC jsonb is parsed with the zod schema in lib/dashboard/schema.ts (tested). A null or failed parse shows the notMember copy instead of crashing.
- Extra reads: `match_results`, `match_participants` (filtered to my player_id), the badges catalog, and my own players row. All are RLS-scoped, and nothing belongs to other users beyond match scores the group can already see. This deviation is justified (the RPC lacks score and W/D/L).
- OVR chart: a client Recharts component, responsive via ChartContainer, with an empty state below 2 points (tested). TopBar has a back link to /g.
- Copy comes from es.dashboard.* in voseo. No `any`, no console.log.

## Notes
- Consider extending `get_my_dashboard.recent_matches` with the score, winner_side and my side, which would drop the 2 extra queries (the worker flagged this).
