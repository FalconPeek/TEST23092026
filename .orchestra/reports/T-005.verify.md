RESULT: PASS

## Checks
- `npx vitest run lib/actions`: 2 files, 35 tests passed.
- `npm run lint`: clean. `npm run typecheck`: passed. `npm test`: 311/311 passed.
- Changed files: messages/es.ts plus the 4 new lib/actions files. All are declared in the task (the other modified files are .orchestra bookkeeping).
- Reviewed the code for all 14 actions. Each one does zod safeParse, then getUserId, then calls the RPC through the session client (no admin client), then revalidatePath and returns an ActionResult. None of them return raw DB errors; they go through mapDbError. There is no `any` and no console.log.
- Role enum is member/spectator/admin, so `owner` is rejected at parse time.
- Checked the mapDbError substrings against the `raise exception` texts in 20260923055124_group_rpcs.sql. All of them match: revoked, expired, usage limit, "transfer ownership before leaving", "the owner cannot be removed".
- The es.ts strings match the spec exactly and use voseo.
- updateGroup parses settings with groupSettingsSchema so defaults are applied. createInvite sends an explicit null for "no expiry/limit", and the invite URL is built with siteUrl() (which ends in a trailing slash).

## Notes (not failures)
- For revoke/claim/assign/updatePlayer, `groupId` comes from the client and is only used for revalidatePath. That's harmless because authorization happens in the RPC.
- The `as unknown as` casts for nullable RPC args are there to work around codegen typing. Worth revisiting if the generated types improve.
