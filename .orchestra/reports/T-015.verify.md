RESULT: PASS

## Checks
- `npx vitest run components/match`: 6 files, 27 tests passed, including match-result.test.tsx and resolve-dispute-form.test.tsx.
- `npm run lint`: clean. `npm test`: 652 passed, 3 skipped, 66 files. `npm run build`: succeeded.
- `npm run typecheck`: the only errors are in `e2e/match-flow.spec.ts` ('match' is possibly null, at lines 121–122). That's FATHER's in-progress M6 e2e work. The same goes for `playwright.config.ts` and the `e2e/` changes: they aren't worker files and aren't in the worker report. All worker files type-check clean.
- Changed worker files are all declared in the task. stats-form.tsx was only changed to use the extracted stat-row.tsx.
- Admin gating:
  - FinalizeButton renders only for admins in reporting/pending_finalize.
  - ResolveDisputeForm renders only for admins in disputed, and non-admins get the disputed banner instead. The resolve form has the same banner built in.
  - The server also re-checks: finalizeMatchNow checks admin explicitly, and resolve_dispute checks it in the RPC.
- The finalized view uses only `match_results` and `match_stats` (reconciled data), never raw reports. The MVP highlight, pens formatting and clean-sheet shield are tested.
- ResolveDisputeForm sends overrides only for rows that were edited, and `stats: undefined` when none were (tested). It has a confirm dialog.
- FinalizeButton has a confirm dialog. It toasts success on finalized, adds a warning toast when `tournamentSyncError` is set, and toasts the disputed title on disputed. Then it calls router.refresh().
- pending_finalize with the rating window closed shows the waitingRatings message. The matches list shows pens.
- Every tap target is ≥44px (steppers size-11, the toggle label min-h-11, the main buttons h-11).
- Copy comes from es.match.* in voseo. No `any`, no console.log.

## Notes (non-blocking)
- The admin's resolve form is prefilled from the admin's *own* reports, which is the only thing RLS allows. That's correct, but admins who didn't report start from empty or zero values.
