RESULT: PASS

## Checks
- `npm run lint`: clean. `npm run typecheck`: passed. `npm test`: 313/313 passed (33 files, including the 2 new bottom-nav tests). `npm run build`: succeeded, and /g, /g/[groupId] and the partidos/torneos/rankings routes are all dynamic.
- Changed files are all declared in the task: app/page.tsx, messages/es.ts, app/g/**, components/app-shell/*, components/groups/create-group-dialog.tsx.
- Non-member 404: the layout reads `groups` through the session client, and the RLS policy `groups_select_member` uses `private.is_member(id)`. If the query errors or returns no row, the layout calls notFound(). It doesn't distinguish "not found" from "not yours", so nothing leaks. Child pages sit under this layout.
- The new es.ts strings match the spec and use voseo. `members(n)` handles singular and plural. I found no hard-coded UI copy.
- The layout has mobile-first styling. The bottom nav is fixed and uses the safe-area inset. The group layout's `<main>` has pb-20, so content isn't hidden under the nav. The top bar is sticky.
- TopBar reads the profile server-side, and sign-out uses the signOut server action. The create dialog calls the createGroup action and shows error toasts with mapped (non-raw) messages.
- No `any`, no console.log, no admin client.
- I did no manual browser check: the extension isn't available and there's no E2E user.

## Notes (not failures)
1. The member count comes from active `players` rows, which include guests (`is_guest`). "N miembros" may therefore overcount real members. Consider filtering `is_guest = false` or relabelling later.
2. The bottom nav links to `/g/[id]/ajustes` and the members preview links there too, but that page doesn't exist yet, so the link 404s for now. Out of scope here; a later task needs to add it.
3. The group home has no in-page group-name header; the name only appears in the TopBar title. That's acceptable.
