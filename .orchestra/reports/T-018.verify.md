RESULT: FAIL

## Checks
- `npx vitest run lib/tournament components/tournament`: 5 files, 43 tests passed.
- `npm run lint`: clean. `npx tsc --noEmit`: clean. `npm test`: 715 passed, 3 skipped, 71 files. `npm run build`: succeeded, and /llave is dynamic.
- Changed files are all declared in the task.
- Realtime (OK):
  - Uses the browser client and subscribes to `tournament_matches` (filter `tournament_id=eq.<id>`; that column exists in the types) and to `tournaments` (`id=eq.<id>`).
  - Refreshes are debounced at 500 ms, and on unmount the timeout is cleared and `removeChannel` is called.
  - Skipping the `matches` subscription is justified: that table has no tournament_id filter, and the finalizer syncs tournament_matches anyway.
- Admin gating (OK): MatchAdminMenu is passed `admin` from the group role. Tests cover pens fields showing only on a tied KO and a walkover requiring a winner.
- Layout (OK): the bracket columns scroll inside an `overflow-x-auto` snap container with `-mx-4 px-4`, so only the container scrolls. Byes and TBD slots come from bracket-layout's resolveSlot.
- Per-match winner display (OK): it comes from `winner_entry_id`.

## Problems
1. **The champion is shown prematurely, and is wrong, while a double-elim grand-final reset is pending.**
   - Where: `lib/tournament/bracket-layout.ts:204-205` computes `championEntryId` as the winner of the highest-round *completed* `final` match. The llave page (`app/g/[groupId]/torneos/[tournamentId]/llave/page.tsx:59-70`) passes `championName` to BracketView regardless of tournament status.
   - Reproduction: I ran a scratch test (deleted afterwards). generateDoubleElim(4), simulate to the GF, then have the losers-bracket entry win GF1 (score 0-1), which activates the reset. `buildBracketLayout(...).championEntryId` is `'e2'` (GF1's winner) while GF2 is still `ready`. The banner declares a champion who may still lose the reset.
   - The spec says to show the "champion banner when finished". Expected: show the champion only when the tournament status is `finished`. Load `status` in the llave page and pass championName only if it's `finished`. Alternatively, have the layout return null when any `final` match is still ready, waiting or in progress.
   - Add a test for the pending-reset case (championEntryId must be null).
   - The same gate also fixes the minor case of single-elim with a pending third-place match, which currently announces the champion before the tournament is finished.
