RESULT: PASS

## Checks
- `npx vitest run components/settings components/player components/scouting lib/actions`: 14 files, 188 tests passed.
- `npm run lint`: clean. `npx tsc --noEmit`: clean. `npm test`: 782 passed, 3 skipped, 78 files. `npm run build`: succeeded.
- Changed files are within the declared list. The extra modified test files only had their per-file ResizeObserver/pointer stubs removed, which the task explicitly allows. `lib/actions/groups*` were untouched, as justified.

## Step by step
1. Settings number fields: raw strings are kept in state, so a cleared field shows `groupSettings.required` and blocks submit. Reset clears the raw values.
2. Advanced labels come from `es.groupSettings.advanced`, with a humanized-key fallback.
3. The worker's SQL claim is correct: `private.apply_player_update` overwrites preferred_foot and height_cm unconditionally. Clearing height already worked, and the "Sin especificar" foot option was added. No SQL change, per the fallback instruction. Tested.
4. The transfer select lists only admins and members.
5. Position chips use `es.positions`, the bars use new `--stat-*` theme tokens, and the empty state uses `profile.noStats`.
6. The rating-integrity fix is confirmed. Quick-mode state starts from the prefill only, sends only touched or prefilled keys, and requires at least 1. Tests cover no implicit default votes, prefill counting as touched, and the save being disabled until something is touched.
7. The stubs are centralized in vitest.setup.ts; no component or lib test still defines ResizeObserver or hasPointerCapture.
8. `matches.startReportingConfirm` is used in lineup-editor.
9. Login inputs and buttons are h-11. The steppers in score-form and stat-row have `match.decrease`/`increase` aria-labels, and the e2e selector was updated to "Sumar a Blancos/Negros".
10. `issue.message` is no longer rendered anywhere in components. Errors map from the zod issue code to fieldRequired, fieldRange or fieldInvalid.
11. The GK quick tab shows VEL/EST/MAN/SAQ/REF/COL (pac plus the GK_QUICK_KEYS), and outfield targets keep the six. Tested for both, including the GK pac key being sent. The action's zod accepts QUICK_VOTE_KEYS, and the RPC enforces GK-only keys per target.

## Notes (from the worker, for a follow-up)
- match-admin-menu.tsx's local Stepper (T-018) still has unlabeled −/+ buttons.
- The create-tournament-form format radio cards use a `has-[[data-checked]]:ring-2` highlight that may never activate, because the RadioGroup item uses aria-checked/data-state. Worth a visual check.
- I didn't run Playwright for e2e/match-flow.spec.ts (it needs the local stack).
