---
id: T-022
title: Fix T-021 — last two sub-44px tap targets (standout chips, saves toggle)
assignee: worker
status: verified
depends_on: [T-013]
---

## Context
T-021 (the fix for rejected T-014) was **rejected** narrowly; see `.orchestra/reports/T-021.verify.md`. The steppers and search input are fixed and build/tests are green. Two controls still break the T-014 criterion "every stepper, rating button and input ≥44px". Keep all T-014/T-021 files in place and fix them in place.

## Steps
1. `components/match/rating-form.tsx`: the standout attribute chips use `py-0.5` (~22px tall). Make each chip a ≥44px tap target (`min-h-11`, with padding adjusted so labels stay centered). Wrapping to multiple lines is fine.
2. `components/match/stats-form.tsx` (~lines 121–124): make the "mostrar atajadas" checkbox + label row a ≥44px tap target. Wrap it in a `label` with `min-h-11 flex items-center gap-2` so the whole row toggles it.
3. Extend the existing tests (`rating-form.test.tsx`, `stats-form.test.tsx`) to assert `min-h-11` on the chips and on the saves-toggle label.
4. Self-check before setting `done`: grep `components/match/{score,stats,rating}-form.tsx` for any interactive element (button, input, label wrapping an input, chip) without a `size-11`/`h-11`/`min-h-11` class, and fix any you find.

## Files
- `components/match/rating-form.tsx`, `components/match/rating-form.test.tsx`
- `components/match/stats-form.tsx`, `components/match/stats-form.test.tsx`
- `components/match/score-form.tsx` (only if step 4 finds something)

## Acceptance criteria
- Every interactive control in the three match forms is ≥44px, and all other T-014 criteria still hold.
- Set only T-022 to `done`. FATHER commits T-014 + T-021 + T-022 together after verification.

## How to verify
- `npx vitest run components/match lib/match`
- `npm run lint && npm run typecheck && npm test && npm run build`
