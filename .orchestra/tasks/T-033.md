---
id: T-033
title: Polish — tournament result stepper labels, format card selected state
assignee: worker
status: todo
depends_on: [T-032]
---

## Context
Follow-ups reported by the Worker in `.orchestra/reports/T-020.worker.md` and confirmed by the Verifier.

## Steps
1. `components/tournament/match-admin-menu.tsx`: the score/pens −/+ stepper buttons have no accessible name. Add `aria-label`s from `es.ts`, reusing `match.decrease(label)` / `match.increase(label)` added in T-020 (check the exact keys), with the team name or "Penales" as the label. Update `match-admin-menu.test.tsx` to find them by name.
2. `components/tournament/create-tournament-form.tsx`: the format radio cards' selected ring (`has-[[data-checked]]`) may never apply.
   - Check which attribute the radio item actually sets (`data-state="checked"` for Radix-based shadcn) and use the matching variant, e.g. `has-[[data-state=checked]]:ring-2`, or derive the class from the selected value.
   - Add a test asserting the selected card gets the ring class.

3. From `.orchestra/reports/T-029.verify.md`: crest objects must not be orphaned in Storage.
   - In `components/clubs/club-form.tsx`, when a new crest is uploaded with a different extension than the current `crestPath`, remove the old object (`storage.from("club-crests").remove([oldPath])`) after the new one is saved.
   - "Quitar escudo" must also remove the object, not just clear the path.
   - Removal failures are ignored (best effort) and never block saving.
   - Extend `club-form.test.tsx`.

## Files
- `components/clubs/club-form.tsx`, `components/clubs/club-form.test.tsx`
- `components/tournament/match-admin-menu.tsx`, `components/tournament/match-admin-menu.test.tsx`
- `components/tournament/create-tournament-form.tsx`, `components/tournament/create-tournament-form.test.tsx`
- `messages/es.ts` (only if a label key is missing)

## Acceptance criteria
- Every −/+ control in the app has an accessible name, and the selected tournament format is visibly highlighted.

## How to verify
- `npx vitest run components/tournament`
- `npm run lint && npm run typecheck && npm test`
