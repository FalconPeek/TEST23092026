---
id: T-021
title: Fix T-014 — ≥44px tap targets in stats form and standout search
assignee: worker
status: todo
depends_on: [T-013]
---

## Context
T-014 (match detail: report score/stats, rate players) was **rejected**; see `.orchestra/reports/T-014.verify.md`. Everything else passed (privacy, gating, payloads, tests). Only the tap-target acceptance criterion ("steppers and rating buttons are ≥44px tap targets") failed. The T-014 files are still uncommitted in the working tree; keep them and fix in place.

## Steps
1. `components/match/stats-form.tsx` (lines ~51 and ~60): the −/+ stepper buttons are `size-6` (24px). Make them ≥44px (`size-11`, or `min-h-11 min-w-11`) and adjust the row layout so the 4 steppers still fit at 375px without horizontal scroll. For example, per player: the name on top, then a 2×2 grid of stat steppers (label + − value +).
2. `components/match/rating-form.tsx` (line ~98): the standout search input is `h-8`; make it `h-11`.
3. Add a regression test in `components/match/rating-form.test.tsx` (and a new `components/match/stats-form.test.tsx`) that asserts the stepper buttons / search input carry the `size-11`/`h-11` (or `min-h-11`) classes.

## Files
- `components/match/stats-form.tsx`, `components/match/stats-form.test.tsx` (new)
- `components/match/rating-form.tsx`, `components/match/rating-form.test.tsx`
- plus the other T-014 files, only if the layout change requires it: `app/g/[groupId]/partidos/[matchId]/page.tsx`, `components/match/score-form.tsx`, `lib/match/report.ts`, `lib/match/report.test.ts`, `messages/es.ts`

## Acceptance criteria
- All T-014 acceptance criteria hold, including every stepper, rating button and input in the match detail forms being ≥44px tall/wide.
- There's no horizontal scroll at 375px.
- When done, set only T-021 to `done` and leave T-014's status alone. The Verifier checks T-021 against T-014's full criteria, and FATHER commits both together.

## How to verify
- `npx vitest run lib/match components/match`
- `npm run lint && npm run typecheck && npm test && npm run build`. If the build fails only because of an in-progress FATHER file under `lib/server/` or `lib/actions/tournaments.ts`, note it in your report; it isn't a blocker for this task.
