---
id: T-020
title: Polish — Verifier notes (T-008, T-009, T-011, T-012 quick-vote bias)
assignee: worker
status: verified
depends_on: [T-027]
---

## Context
These are non-blocking notes collected by the Verifier (see `.orchestra/reports/T-008.verify.md` and `T-009.verify.md`).

## Steps
1. `components/settings/group-settings-form.tsx`: a cleared number input must not become `0`. Keep the raw string in form state (or `NaN`) so `safeParse` reports a field error ("Completá este campo"; add `groupSettings.required` to `es.ts`).
2. "Avanzado" labels: add `es.groupSettings.advanced` with Spanish labels for every numeric `rating.*` leaf in `lib/settings/group.ts`. The form uses the label when present and falls back to the humanized key, so future tunables still render.
3. `components/player/player-profile-form.tsx` + `lib/actions/groups.ts` `updateMyPlayer`/`updatePlayer`: allow clearing height and preferred foot.
   - First read `private.apply_player_update` in `supabase/migrations/*group_rpcs.sql` to see how null is treated.
   - If the RPC can't distinguish "clear" from "keep", do NOT change SQL: note it in your report and implement only the UI side (explicit "Sin especificar" option) that sends what the RPC supports.
4. Transfer-ownership select (`components/settings/danger-zone.tsx`): list admins and members only (exclude spectators), for a clearer UX.
5. From `.orchestra/reports/T-011.verify.md`:
   - The profile's "Media por posición" chips show the raw code; label them with `es.positions`.
   - The attribute bars use raw Tailwind colors; switch to theme tokens (add `--stat-low/mid/good/great` in `app/globals.css` `@theme` if missing).
   - The empty stats state reuses `groups.noMatches`; add `profile.noStats`.
6. From `.orchestra/reports/T-012.verify.md` (this one changes rating integrity, so it matters most):
   - In quick mode, untouched sliders currently send a default of 5, which biases cards toward 62.5.
   - Quick mode must behave like detailed mode: untouched face stats stay "sin votar" and are NOT sent, with at least 1 touched required to save.
   - Prefilled values from the existing ballot count as touched.
   - Update `components/scouting/scouting-form.tsx` and its test. This supersedes T-012's "quick mode always sends all 6".
7. Test infra: move the per-file ResizeObserver / pointer-capture stubs used by the slider/radar tests into the shared `vitest.setup.ts`, and remove the per-file copies.
8. From `.orchestra/reports/T-013.verify.md`: the "Terminó el partido" confirm dialog reuses `matches.cancelConfirm`. Add `matches.startReportingConfirm` ("Se cierra el partido y se abre la carga de resultados y puntajes. ¿Seguro?") and use it in `components/match/lineup-editor.tsx`.
9. Found by FATHER while writing e2e tests:
   - `components/auth/login-form.tsx`: the email/password inputs use the default `h-8` (32px). Make every input and button on the login page ≥44px (`h-11`).
   - The −/+ stepper buttons in `components/match/score-form.tsx` and `components/match/stat-row.tsx` have no accessible name (screen readers announce "minus"/"plus"). Add `aria-label`s from `es.ts`, e.g. `match.decrease(label)` "Restar a {label}" and `match.increase(label)` "Sumar a {label}".
   - If you change button names, keep `e2e/match-flow.spec.ts` working: it locates the score steppers by the name "+". Update that selector to the new aria-label.
10. From `.orchestra/reports/T-016.verify.md`: `components/tournament/create-tournament-form.tsx` and `components/settings/group-settings-form.tsx` show raw English zod issue messages as field errors. Map issues to Spanish instead: add `errors.fieldInvalid` "Valor inválido", `errors.fieldRange(min, max)` "Tiene que estar entre {min} y {max}", and `errors.fieldRequired` "Completá este campo", chosen from the zod issue `code`. Never render `issue.message`.
11. Goalkeeper quick mode (FATHER backend done in 13febb1): for GK targets, the quick tab of `components/scouting/scouting-form.tsx` must show the card's GK stats instead of the outfield six: VEL (`pac`), EST (`div`), MAN (`han`), SAQ (`kic`), REF (`ref`), COL (`pos`). Use the labels from `es.card.gkStats` (and `es.card.gkStats.spd` for `pac`). The keys come from `QUICK_VOTE_KEYS` / `GK_QUICK_KEYS` in `lib/rating/attributes.ts`. The same "only touched keys are sent" rule from step 6 applies. Outfield targets keep the 6 outfield sliders. Add a test for each case.
12. Update the related tests.

## Files
- `messages/es.ts`
- `components/settings/group-settings-form.tsx`, `components/settings/group-settings-form.test.tsx`
- `components/player/player-profile-form.tsx`, `components/player/player-profile-form.test.tsx`
- `lib/actions/groups.ts`, `lib/actions/groups.test.ts` (only if needed for step 3)
- `components/settings/danger-zone.tsx`, `components/match/lineup-editor.tsx`
- `components/auth/login-form.tsx`, `components/match/score-form.tsx`, `components/match/stat-row.tsx`, `e2e/match-flow.spec.ts` (selector only)
- `components/tournament/create-tournament-form.tsx`, `components/tournament/create-tournament-form.test.tsx`
- `components/scouting/scouting-form.tsx`, `components/scouting/scouting-form.test.tsx`, `components/scouting/vote-slider.test.tsx`, `vitest.setup.ts`, and any test file that carries its own ResizeObserver/pointer stub
- `app/g/[groupId]/jugadores/[playerId]/page.tsx`, `components/player/attribute-list.tsx`, `components/player/attribute-list.test.tsx`, `app/globals.css`

## Acceptance criteria
- Clearing a settings number field shows an error and blocks submit.
- Advanced settings show Spanish labels.
- Quick-mode scouting sends only touched (or prefilled) face stats; there are no implicit default votes.

## How to verify
- `npx vitest run components/settings components/player components/scouting lib/actions`
- `npm run lint && npm run typecheck && npm test`
